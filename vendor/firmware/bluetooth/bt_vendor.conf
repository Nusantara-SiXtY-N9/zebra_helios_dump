# UART device port where Bluetooth controller is attached
UartPort = /dev/ttyHS0

# Base Firmware patch file location kept for MTP
FwPatchFilePath = /vendor/firmware/

# Firmware patch file name
FwPatchFileName = Type_1BB.hcd

# Aslan Firmware patch file location
AslanFwPatchFilePath = /vendor/firmware/aslan/

# Gambit Firmware patch file
GambitFwPatchFilePath = /vendor/firmware/gambit/
